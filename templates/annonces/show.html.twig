{% extends 'base.html.twig' %}

{% block title %}Annonces{% endblock %}

{% block body %}
    <h1>Annonces</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Description</th>
                <td>{{ annonce["description"] }}</td>
            </tr>

            <tr>
                <th>Energie</th>
                <td>{{ annonce["energie"]["libelle"] }}</td>
            </tr>

            <tr>
                <th>Photos</th>
                <td>
                {% for photo in annonce["photo"] %}
                    <img id="icone" src="{{ asset('uploads/') }}{{ photo["url"] }}">
                {% endfor %}
                </td>
            </tr>

        </tbody>
    </table>
    <a href="{{ path('location_new', {'idannonce': annonce.id}) }}">Louer</a>
    <a href="{{ path('annonces_index') }}">back to list</a>

    <a href="{{ path('annonces_edit', {'id': annonce.id}) }}">edit</a>

    {{ include('annonces/_delete_form.html.twig') }}
{% endblock %}
